1.Create multiple lambda functions using for_each


2.use of map(object)


3.validation block to fix input variable like for python runtime versions which can be used.


Terraform variable validation is ALL-or-NOTHING

If any single element in the variable fails validation:

Terraform fails the entire variable

It does not tell you which key failed

It shows one generic error message


4.lifecycle
